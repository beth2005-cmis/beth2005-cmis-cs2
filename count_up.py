def adder(total, s):
    if s == "":
        print "Running total is " + str(total) + "."
    else:
        return total + s            
    print "The sum is " + str(adder) + "."
adder(0, raw_input)
